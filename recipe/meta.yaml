{% set name = "funsor" %}
{% set version = "0.4.7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: a9528f1b399a5c72e9e52169f6a607cc52163751701726422ed9c7f83d404cb5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - pip
  run:
    - python >={{ python_min }}
    - makefun
    - multipledispatch
    - numpy >=1.7
    - opt_einsum >=2.3.2
    - typing_extensions

test:
  imports:
    - funsor
  requires:
    - pip
    - python {{ python_min }}
  commands:
    - pip check

about:
  home: https://github.com/pyro-ppl/funsor
  summary: Funsor is a tensor-like library for functions and distributions.
  description: |
    Funsor is a tensor-like library for functions and distributions.
    See [Functional tensors for probabilistic programming](https://arxiv.org/abs/1910.10775) for a system description.
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE.md
  doc_url: https://funsor.pyro.ai/en/stable/
  dev_url: https://github.com/pyro-ppl/funsor

extra:
  recipe-maintainers:
    - wckdouglas
